config = {
    "project_name": "Python Skeleton",  # This is shown when backend api direct requested on /
    "version": "1.0",  # This is shown when backend api direct requested on /
    "password_salt": 10
}
